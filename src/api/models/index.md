# 通用数据结构

## 用户

```json
{
    "user_id": "string / 用户ID / users.id",
    "user_name": "string / 用户名 / users.name", //
    "user_mobile": "string / 用户脱敏手机号，如：133****3333 / users.mobile",
    "user_gender": "string / 性别 / users.gender",
    "user_gender_desc": "string / 性别文字描述，如：男, 具体描述值见 users.gender / users.gender",
    "posts": { // 这个是示例信息，自行删除
        // 通用数据结构 > 文章
    },
}
```
